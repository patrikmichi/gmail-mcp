# Copy to .env.local (local) or set in Vercel (deploy). Never commit real values.

# MCP: credentials are passed via Authorization header — no env needed for MCP.

# Send-email webhook (POST /api/send-email): set these when using the webhook.
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REFRESH_TOKEN=

# Optional: require Authorization: Bearer <WEBHOOK_SECRET> on POST /api/send-email
# WEBHOOK_SECRET=

# Optional: only if using a different OAuth redirect (e.g. custom domain)
# GMAIL_REDIRECT_URI=http://localhost:3000/oauth2callback
